#include<stdio.h>
int main()
    {
        int length,breadth,area;
        printf("Enter length ,breadth of rectanle :");
        scanf("%d %d",&length,&breadth);
        area = length * breadth;
        printf("area is %d",area);
        return 0;

    }
